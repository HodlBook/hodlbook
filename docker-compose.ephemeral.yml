version: '3.8'

services:
  hodlbook:
    build: .
    ports:
      - "2008:2008"
    environment:
      - APP_ENV=production
      - APP_PORT=2008
      - DB_TYPE=sqlite
      - DB_PATH=/app/data/hodlbook.db
      - DEFAULT_CURRENCY=USD
    restart: unless-stopped
